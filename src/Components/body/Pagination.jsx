// import React from "react";
// import style from './body2.module.css'

// function Pagination({ totalpages, handlePageChange, currentPage }) {
//   const arr = new Array(totalpages).fill(0);
//   return (
//     <div className={style.Flex}>
//       {arr.map((item,i)=>(
//         <button style={{ background: currentPage === i ? "#7f828d" : "#dfdfe2" }} className={style.button} key={i} onClick={(e)=>{handlePageChange(i,e)}} disabled={currentPage===i+1}></button>
//       ))}
//     </div>
//   );
// }

// export default Pagination;
